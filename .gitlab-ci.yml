---
stages:
  - tests

tests:
  stage: tests
  needs: []
  image: python:3.6
  before_script:
    - apt-get -q update
    - apt-get -y -q --no-install-recommends install postgresql
    - pip install tox
    - useradd --create-home gitlab
  script:
    - su -c tox gitlab
